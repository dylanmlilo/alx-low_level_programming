#include "main.h"
/**
 * mul -multiplies int a and in b
 * @a: integer to be multiplied with b
 * @b: integer to be multiplied with a
 *
 * Return: answer
 */
int mul(int a, int b)
{
	int ans;

	ans = a * b;
	return (ans);
}
